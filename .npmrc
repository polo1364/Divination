# npm 配置文件
# 抑制 production 標誌的棄用警告
# Railway 會自動使用 --omit=dev 來跳過開發依賴

# 抑制 npm warn config production 警告
# 這是因為 npm 建議使用 --omit=dev 而不是 --production 標誌
# 我們已經在所有配置文件中使用了 --omit=dev
fund=false
audit=false

